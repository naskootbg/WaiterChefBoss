@model OrderFormViewModel

@{
    ViewData["Title"] = $"Order {Model.Id}";
}
@if (Model.Products.Count() > 0)
{



    <table class="table table-striped grid-table">
        <thead>
            <tr>
                <th>Product</th>
                <th>Price</th>
            </tr>
        </thead>
        <tbody>
            @{
                double total = 0;
            }
            @foreach (var product in Model.Products)
            {
                total += @product.Price;

                <tr>
                    <th>@product.Name</th>
                    <th> @product.Price @product.CurencySymbol</th>
                </tr>
            }
            <tr></tr>
            <tr>
                <th>Table: @Model.Table</th>
                <th>Total: @Math.Round(total,2) @Model.Products.FirstOrDefault()!.CurencySymbol</th>
            </tr>
        </tbody>
    </table>


    @*<form asp-controller="Order" asp-action="PlaceOrder" asp-route-orderId="@Model.Products.FirstOrDefault()!.OrderProductId" asp-route-table="1" method="post">
        <input class="btn btn-dark mb-2 w-100 p-3 fw-bold" type="submit" value="Send to the Waiter" />
    </form>*@
}

